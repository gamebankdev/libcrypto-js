(function(root, factory) {
  if (typeof exports === 'object' && typeof exports.nodeName !== 'string') {
    /* global require */
    factory(exports);
  } else {
    root.gamebank = root.gamebank || {};
    factory((root.gamebank.sjcl = {}));
  }
})(this, function(exports) {

  // INSERT

});
